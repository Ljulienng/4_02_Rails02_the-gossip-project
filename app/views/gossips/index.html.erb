<body>

  <div class="container h-50">
    <div class="card-columns" style="max-height: 50%">
      <% @gossips.each do |gossip|%>
          <div class="card" style="width: 18rem;">
            <div class="card-body">
              <h5 class="card-title" style="white-space: nowrap"><%=gossip.author.first_name%>'s gossip</h5>
              <p class="card-text"><%=gossip.title%></p>
              <% if gossip.likes.size > 0 %>
                <p class = "card-text"><%= gossip.likes.size %> 💖</p>
              <% end %>
              <%= link_to "Check it out", user_gossip_path(params[:user_id],gossip.id), :class => "btn btn-dark" %>
              <% @current_gossip_id = gossip.id %>
              <%= button_to '💖 LIKE 💖', user_gossip_likes_path(params[:user_id], gossip.id, :url => "gossip_index"), method: :post, class: "btn btn-dark" %>
            </div>
          </div>
      <% end %>
    </div>
  </div>

</body>