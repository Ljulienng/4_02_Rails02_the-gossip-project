
<div class="card">
  <div class="card-header">
    Gossip n°<%=params[:id]%>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%=Gossip.find(params[:id]).author.first_name %></h5>
    <p class="card-text"><%=Gossip.find(params[:id]).content%></p>
    <a href="/welcome/back" class="">Back to gossip list</a>
  </div>
</div>


